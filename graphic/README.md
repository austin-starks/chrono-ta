# README animation

This Remotion composition explains the semantic difference between upstream
`ta` and `chrono-ta`: observation-count windows versus elapsed-time windows.

```bash
npm ci
npm run check
npm run render-gif
```

The committed artifact is `out/window-semantics.gif`. The source stays in the
repository so the animation can evolve with the crate's behavior.
